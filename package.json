{
  "name": "healint-server",
  "version": "1.0.0",
  "description": "A barebones server to serve up JSON as API endpoints for healint-client",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Parag Bhatnagar",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "pm2": "^2.9.1"
  }
}
